{% extends 'base.html.twig' %}

{% block title %}Gestion de mes Animaux • Accueil{% endblock %}

{% block body %}

<div class="wrapper">
{% if is_granted('IS_AUTHENTICATED_FULLY') %}
    <h2 class="home-title-top text-white">Accueil</h2>
    <a class="home-option" href="{{ path('app_animal_list') }}">Gérer mes animaux</a>
    <a class="home-option" href="{{ path('app_event_list') }}">Gérer mes évènements</a>
    <a class="home-option" href="{{ path('app_calendar_show') }}">Afficher le calendrier</a>
    <a class="home-option" href="{{ path('app_mail_manager') }}">Gérer mes emails</a>

{% else %}
<h2 class="home-title-top text-white">Veuillez vous connecter</h2>
 {% endif %}
</div>
{% endblock %}